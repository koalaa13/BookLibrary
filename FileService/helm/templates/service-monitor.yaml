apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: file-service-metrics-monitor
  labels:
    app: file-service-metrics-monitor
spec:
  endpoints:
    - port: file-service-metrics
      scrapeTimeout: 30s
      path: /actuator/prometheus
      honorLabels: true
      scheme: http
  namespaceSelector:
    matchNames:
      - default
  selector:
    matchLabels:
      app: file-service